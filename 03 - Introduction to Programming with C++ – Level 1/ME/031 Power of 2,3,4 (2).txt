#include <iostream>
#include <cmath>
using namespace std;

int main() {
    float Num, a, b, c;

    cin >> Num;

    a = Num * Num;
    b = Num * Num * Num;
    c = Num * Num * Num * Num;

    cout << round(a) << endl;
    cout << round(b) << endl;
    cout << round(c) << endl;

    return 0;
}