#include <iostream>
using namespace std;

int main() {
    double a, b;
    cin >> a >> b;

    double PI = 3.14;
    double Area = (PI * b * b / 4) * ((2 * a - b) / (2 * a + b));

    cout << Area;

    return 0;
}