#include <iostream>
#include <cmath>   
using namespace std;

int main() {
    float L, Area;
    const float PI = 3.14;

    cout << "Enter L: ";
    cin >> L;

    Area = (L * L) / (4 * PI);

    cout << "Area = " << floor(Area) << endl;

    return 0;
}