#include <iostream>
#include <cmath>   // مهم عشان ceil
using namespace std;

int main() {
    float A, Area;
    const float PI = 3.14;

    cout << "Enter A: ";
    cin >> A;

    Area = (PI * A * A) / 4;

    cout << "Area = " << ceil(Area) << endl;

    return 0;
}