#include <iostream>
#include <cmath>
using namespace std;

int main() {
    float a, b, Area;
    const float PI = 3.14;

    cin >> a >> b;

    Area = (PI * b * b / 4) * ((2 * a - b) / (2 * a + b));

    cout << floor(Area) << endl;

    return 0;
}