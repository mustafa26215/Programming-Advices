#include <iostream>
using namespace std;

int main() {
    int x, sum = 0;

    for (int i = 0; i < 5; i++) {
        cin >> x;
        if (x >= 50) continue;
        sum += x;
    }

    cout << sum;

    return 0;
}